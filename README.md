# Nook Waiter Web App

Веб-приложение для официантов ресторанов в системе Nook - платформе лояльности с персонализированным обслуживанием.

## Функциональность

### 🔍 Идентификация гостей
- **QR-сканер** через камеру браузера для мгновенной идентификации
- **Поиск по телефону** как альтернативный способ
- **Полный профиль гостя** с историей посещений и предпочтениями

### 👥 Управление столами
- **Активные столы** - список всех обслуживаемых гостей
- **Время обслуживания** - отслеживание длительности визита
- **Информация о гостях** - размер компании и детали

### 🎁 Персональные предложения
- **Автоматические рекомендации** на основе истории гостя
- **Применение акций** одним кликом
- **Отслеживание использованных предложений**

### 📝 Заметки и наблюдения
- **Предпочтения гостей** - диетические ограничения, любимые блюда
- **Предупреждения** - аллергии и важные замечания
- **Заметки персонала** - наблюдения для улучшения сервиса

## Технические особенности

### Поддерживаемые браузеры
- ✅ Chrome 80+ (рекомендуется)
- ✅ Firefox 75+
- ✅ Safari 13+
- ✅ Edge 80+

### QR-сканер
- Использует **ZXing библиотеку** для распознавания QR-кодов
- Поддержка **камеры устройства** (задняя камера на мобильных)
- **Автоматическое определение** формата QR-кода
- **Офлайн-обработка** - не требует интернета для сканирования

### Адаптивный дизайн
- **Мобильная оптимизация** для работы на планшетах и телефонах
- **Современный UI** с плавными переходами
- **Темная тема** для QR-сканера

## Установка и настройка

### 1. Загрузка файлов
```bash
git clone https://github.com/gsabanti/nook-waiter-web.git
cd nook-waiter-web
```

### 2. Настройка конфигурации
Отредактируйте `config.js`:
```javascript
const CONFIG = {
    API_BASE_URL: 'https://api.sabanti.tech/api/v1',
    DEFAULT_RESTAURANT_ID: 'your-restaurant-id-here'
};
```

### 3. Развертывание
Просто скопируйте файлы на веб-сервер:
```bash
# Nginx пример
cp -r * /var/www/html/waiter/

# Apache пример  
cp -r * /var/www/html/waiter/
```

### 4. Настройка веб-сервера
Для работы QR-сканера требуется **HTTPS**:

**Nginx конфигурация:**
```nginx
server {
    listen 443 ssl;
    server_name yourdomain.com;
    
    location /waiter {
        root /var/www/html;
        index index.html;
        try_files $uri $uri/ /waiter/index.html;
    }
}
```

## Использование

### 1. Авторизация
Официант входит в систему используя:
- **Телефон** - тот же, что указан в системе персонала
- **Пароль** - установленный администратором

### 2. Работа с гостями
1. **Сканирование QR-кода:**
   - Нажать "Сканировать гостя"
   - Навести камеру на QR-код гостя
   - Профиль загрузится автоматически

2. **Поиск по телефону:**
   - Нажать "Ввести телефон вручную"
   - Ввести номер телефона гостя
   - Нажать "Найти гостя"

### 3. Начало обслуживания
1. В профиле гостя нажать "Начать обслуживание"
2. Указать номер стола и количество гостей
3. Опционально выбрать повод визита
4. Подтвердить начало обслуживания

### 4. Управление предложениями
- **Доступные акции** показываются автоматически
- **Применение предложения** одним кликом
- **Уведомление** о успешном применении

## API Integration

Приложение интегрируется с Nook API:

### Основные endpoints:
- `POST /staff/login` - авторизация
- `POST /staff/scan-qr` - сканирование QR-кода
- `POST /staff/identify-guest` - поиск по телефону
- `POST /staff/start-visit` - начало визита
- `GET /staff/my-tables` - активные столы
- `GET /offers/available` - доступные предложения

## Безопасность

### HTTPS обязателен
- QR-сканер требует **защищенного соединения**
- Данные гостей передаются **зашифровано**

### Аутентификация
- **JWT токены** для авторизации (планируется)
- **Сессии** с автоматическим истечением
- **Ограниченные права** доступа по ролям

### Приватность
- **Хеширование** номеров телефонов в базе
- **Локальное** хранение сессий
- **Минимальный** объем передаваемых данных

## Производительность

### Оптимизация камеры
- **Адаптивное разрешение** под устройство  
- **Ограниченная область сканирования** для скорости
- **Автоматическая остановка** камеры при переходах

### Кеширование
- **Локальное хранение** сессий
- **Минимизация** API вызовов
- **Автообновление** данных каждые 30 секунд

## Разработка

### Структура проекта
```
nook-waiter-web/
├── index.html          # Главная страница
├── styles.css          # Стили приложения
├── config.js           # Конфигурация
├── api.js              # API клиент
├── qr-scanner.js       # QR сканер
├── app.js              # Основная логика
└── README.md           # Документация
```

### Запуск локально
```bash
# Простой HTTP сервер для тестирования
python3 -m http.server 8080

# Или с помощью Node.js
npx serve .
```

**Внимание:** Для QR-сканера нужен HTTPS даже в разработке!

## Troubleshooting

### QR-сканер не работает
1. Проверьте **HTTPS** соединение
2. Разрешите **доступ к камере** в браузере
3. Убедитесь, что **камера не используется** другим приложением

### Ошибки авторизации
1. Проверьте **правильность данных** для входа
2. Убедитесь, что **API доступен**
3. Проверьте **настройки CORS** на сервере

### Гость не найден
1. Убедитесь, что гость **зарегистрирован** в системе
2. Проверьте **правильность номера телефона**
3. Попробуйте **другой формат** QR-кода

## Поддержка

- **Email:** support@nook.app
- **Telegram:** @nook_support
- **GitHub Issues:** https://github.com/gsabanti/nook-waiter-web/issues

## 🔄 Versioning & Cache Management

### Automatic Versioning System

**Problem:** Browser caching prevents updates from showing immediately.
**Solution:** Timestamp-based cache busting.

### Development Workflow

**Smart Commit (Recommended):**
```bash
./commit.sh "Your commit message"
```
Automatically:
- Updates version numbers (`?v=MMDDHHММ`)
- Copies to production
- Git commit and push with prompts

**Manual Version Update:**
```bash
./update-version.sh
cp *.js *.html /var/www/sabanti_tech/html/waiter/
```

**Production Deployment:**
```bash
sudo ./deploy.sh  # Full deployment with auto-versioning
```

### Versioned Files
- `config.js?v=TIMESTAMP`
- `phone-mask.js?v=TIMESTAMP`
- `api.js?v=TIMESTAMP` 
- `qr-scanner.js?v=TIMESTAMP`
- `app.js?v=TIMESTAMP`
- `styles.css?v=TIMESTAMP`

**Always update versions when making changes to avoid cache issues!** 🚨

---

Создано для системы [Nook](https://nook.app) - платформы лояльности нового поколения 🚀